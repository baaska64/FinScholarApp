# FinScholar - App Context & Roadmap

This document serves as the single source of truth for the app's current state, features, and future roadmap so that the development context is never lost.

## 📱 App Identity
- **App Name:** FinScholar
- **Package Name:** `com.lalex.finscholar`
- **Data & Privacy:** Uses Supabase for backend authentication, but login is completely OPTIONAL. Users can use the app 100% locally. If they choose to log in to sync data, the app collects their Email. Requests Camera permissions strictly for scanning syllabuses/schedules locally.

## ✨ Current Features (V1)
1. **Grade Tracker:** Calculate and track GWA/grades.
2. **Auto-import Schedule:** Uses the camera to scan and parse class schedules.
3. **Auto-import School Calendar:** Uses the camera to scan academic calendars.
4. **Track Tasks:** To-do list for assignments and exams.
5. **Study (flashcards):** Anki-style spaced repetition — FSRS scheduling, Basic / both-ways / cloze / type-in cards, daily limits — plus practice tests, speed match and exam mode.

## 🧭 First-Run Onboarding (Implemented)
- **Route order:** `app/index.tsx` decides where a launch lands. Fresh install with no session and no local ledger → `/onboarding`; anyone else keeps the old behaviour (`/(tabs)` when signed in, `/welcome` otherwise).
- **Intro flow (`app/onboarding.tsx`):** four swipeable slides — meet Fin, feature tour, notification opt-in, local-first/optional-sync. Skippable at any point, then `router.replace('/welcome')` so the sign-in choice is unchanged. It writes no ledger data, so signing in afterwards cannot create a sync conflict.
- **Flags (`services/OnboardingService.ts`, AsyncStorage, local only):**
  - `@onboarding_completed_v1` — intro finished or skipped.
  - `@getting_started_dismissed_v1` — dashboard checklist hidden.
  - `@notification_opt_in_v1` — `granted` / `denied` / `skipped`.
  - `isFirstRun()` is the guard used everywhere: false as soon as the flag is set **or** a `grade_ledger_v2_data` ledger exists, so updating users are never sent back through onboarding.
- **Notification permission is deferred:** `app/_layout.tsx` only calls `NotificationService.ensureAndroidChannel()` on a first run and asks for the OS permission inside the onboarding slide. Existing installs still call `initNotifications()` on launch exactly as before.
- **Quick term setup (`components/onboarding/TermSetupModal.tsx`):** bottom sheet that creates a school year + semester (with optional dates) and selects it, so a new user is not dropped into the raw Academic Terms manager. Reached from the dashboard empty state; "Manage Academic Terms" is still there as a secondary link.
- **Getting Started checklist (`components/onboarding/GettingStartedCard.tsx`):** dashboard card tracking term → subjects → schedule → first task. Auto-hides when complete, dismissible, restorable from Settings.
- **Pure logic lives in `utils/onboardingProgress.ts`** (`suggestSchoolYear`, `deriveOnboardingProgress`, `createTermInLedger`) and is covered by `__tests__/onboarding.test.js`.
- **Settings → Help:** "Take the Guided Tour", "Replay Intro Slides" (`/onboarding?replay=1`, returns via `router.back()`) and "Show Getting Started Checklist".

## 🔦 Guided Spotlight Tour (Implemented)
- **What it is:** a coach-mark overlay that dims and blurs the screen, cuts a lit hole around one real control, pulses a ring on it, and explains it in a card that points at it. It is the primary onboarding guidance; the Getting Started checklist is the passive companion to it.
- **Provider:** `components/spotlight/SpotlightProvider.tsx` sits at the root of `app/_layout.tsx`, wrapping the `Stack` so the overlay paints over screens *and* the custom tab bar. It keeps a registry of targets, drives step state, and measures.
- **Measurement:** targets wrap themselves in `SpotlightTarget` (`collapsable={false}`). The provider measures the target and the overlay container in window coordinates and subtracts, so the highlight lines up regardless of status-bar/edge-to-edge offsets. It retries for ~2.6s while a screen settles; if the control never appears the step still shows, centred and unanchored, so a tour never dead-ends.
- **Cut-out technique:** four **fixed screen-sized** opaque panels, each moved by `translateX`/`translateY` only, form the complement of the highlight rect — so the target stays crisp with no masking library. They overlap in the four quadrants, so they live inside a group that carries the alpha (`opacity` + `needsOffscreenAlphaCompositing`): the group composites once and *then* fades, which is what keeps the dim uniform despite the overlap.
- **Everything that moves runs on the native driver.** Only the hole's *position* is animated (`translateX`/`translateY`); its *size* is plain layout that changes once per step, never per frame. An earlier version animated each panel's `top`/`left`/`width`/`height`, which put a full layout pass on the JS thread every frame and made the transition stutter badly — do not reintroduce that. The scrim is a solid colour rather than a `BlurView` for the same reason: a blur that resizes re-rasterises its backdrop every frame.
- **Step transition (no flicker):** the card's content lags behind the step index. Pressing Next fades the old card out (130ms) while the spotlight glides (300ms, ease-out cubic); only then does the copy swap, and the card is re-seated *while invisible* using the freshly measured rect before fading back in (90ms delay + 200ms). The provider reports `highlightReady`, so the card is never revealed against the previous control's position. A late height correction after reveal glides over 160ms instead of jumping. Card opacity and translation run on the native driver.
- **Card position and card opacity are owned by two separate effects, deliberately.** The card measures itself via `onLayout`, which feeds `cardHeight` and therefore `layout.top`. Putting `layout.top` in the deps of the effect that animates `cardOpacity` makes its cleanup cancel the fade-in mid-delay and strand the card at opacity 0 — an invisible tour. Keep the opacity effect's deps limited to `[canReveal, shown.index]`.
- **Fail-safe:** if a measurement has not landed within 600ms the card reveals anyway and slides into place when the rect arrives, so the tour can never sit there invisible.
- **Geometry** lives in `utils/spotlightGeometry.ts` (`inflateRect`, `resolveTooltipLayout`, `highlightRadius`) — pure, and covered by `__tests__/spotlight.test.js`, including safe-area and small-screen cases.
- **Tours** are declared as data in `constants/tours.ts`:
  - `MAIN_TOUR` (7 steps) — term selector, each of the five non-home tabs, settings. Every target is on the dashboard or the tab bar, which mount together, so the tour never navigates mid-flight.
  - One-step first-visit nudges for Grades, Schedule, Calendar, Tasks and Study, pointing at each screen's primary action.
  - `SPOTLIGHT_IDS` is the single source of target ids; a test asserts every step points at a known id.
- **Triggering:** `useScreenTour(key, steps, ready)` runs a tour once per install, waiting for the screen's data before measuring. Tour completion is stored under `@tour_seen_v1_<key>`; `markExistingUserOnboarded()` presets all of them so upgrading users are never interrupted.
- **Controls:** tap anywhere to advance, Back/Next/Done in the card, "Skip tour", and Android hardware back dismisses the tour instead of navigating behind it.
- **Replay:** Settings → Help → "Take the Guided Tour" clears every tour key, returns to the dashboard and restarts; the Getting Started card also has a "Show me around" button.
- **Adding a target:** wrap the control in `SpotlightTarget` with an id from `SPOTLIGHT_IDS`, then reference that id from a step. Register an id from exactly one mounted place — `Tabs` takes an optional `spotlightId` prop precisely so only the dashboard's term selector registers.
- **Changelog:** `ChangelogModal` no longer fires on a fresh install — it records the current version silently so "What's New" starts from the next update.

## 🎨 Visual Language: Tactile Bento (Implemented)
- **No drop shadows.** Depth comes from a tonal step plus a **2px darker bottom border** on `Card` and on pressables — a "lip" that reads as a solid object resting on the page. Blurred shadows under every element were what made the old screens feel like floating AI-template cards. `Shadows` still exists in `constants/Theme.ts` for legacy screens; do not add it to new dashboard work.
- **Cool indigo-tinted canvas, matched to the entry screens.** Light is `#f4f5fb` with `#e3e5f0` borders — off-white so the page never reads as a blank document, but in the same cool family as the welcome/login hero (`#4f46e5`, `#1e293b`, `#e8e8f4`). Dark is `#12132b` with a `#1b1d3a` / `#262a4d` ladder, matching welcome's `#1a1f3d` card. **A warm off-white was tried and reverted**: it made entering the app from the indigo login screen feel like switching products. Keep new neutrals cool.
- **`theme.lip`** is the bottom-edge colour every raised surface and pressable uses. One token, not a hardcoded pair.
- **Text contrast was raised, not just recoloured.** Light `textTertiary` measured ~2.6:1 on the old page — it is now `#6b665f` (~4.8:1), and dark `textTertiary` lifted to `#7c8494`. These carry 10–11px labels, so they need it.
- **`Brand` / `getBrand(isDark)`** in `constants/Theme.ts` is the **identity** blue, and it is not `primary`. FinScholar is branded azure — Fin sits on `#3991f6` in the launcher icon and on the splash — and that blue appeared literally nowhere once the app opened, which is most of why the dashboard was called plain and generic. `primary` (`#4f46e5` indigo) is wired through every other screen and stays exactly as it is; `Brand` is spent where the product should feel like FinScholar. `base` is the icon's literal azure and is what the washes are mixed from. **`heroFrom` (`#2470e0`) and `heroTo` (`#123f8f`) are deliberately deeper than the icon**: white on the raw `#3991f6` measures 2.8:1 and the band carries 10–12px labels, so `heroFrom` is the same hue family tuned to clear 4.7:1. **Do not lighten `heroFrom` back toward the icon value.** `well` / `wellStrong` / `wellLine` are translucent containers that sit *on* the band, sized for icons and tracks, which owe only 3:1 as graphical objects — **small white text goes straight on the band, never on a well**, because a light well lifts the local background out of contrast. `wash` / `washLine` / `ink` / `solid` are the on-page counterparts for brand-tinted panels, links and accents.
- **`Tints` / `getTints(isDark)`** in `constants/Theme.ts` is the colour code: violet = grades, emerald = attendance, amber = tasks, indigo = schedule, sky = tools, red = danger. Each has `fill` (panel wash), `line` (inner border), `ink` (legible foreground on `fill`) and `solid` (rails, active states). **Use a tint only where it carries meaning** — a domain, a risk level, an active state. Never to decorate.
- **Saturated colour is reserved.** Full-strength colour appears on active P/A/NC buttons, the NOW badge, overdue rows and risk captions. Everything else is a wash.
- **`Card` variants:** `raised` (default object surface), `sunken` (recessed shelf, e.g. the quick-action toolbelt), `flat` (outline only). `fill`/`border` accept a tint for a card that carries meaning.

## 🏠 Dashboard Layout & Design System (Implemented)
- **Shared primitives — use these, do not restyle a box by hand:**
  - `components/ui/Card.tsx` — the one card recipe (surface, hairline border, `Radius.xl`, `Shadows.sm` in light only). `padding={0}` for cards that own their sections; `accent` raises the border for the one card asking for attention; passing `onPress` wraps it in `AnimatedPressable`.
  - `components/ui/ProgressBar.tsx` — every completion bar in the app (semester timeline, subject tasks). Animates width on change; pass `animate={false}` inside horizontal lists.
  - `components/ui/SectionHeader.tsx` — plain type heading with optional `count` pill, `subtitle`, and a text action link. `actionColor` defaults to `primary`; **the dashboard passes `brand.solid`** so its links belong to the azure header above them instead of introducing a second blue.
  - `components/dashboard/HeroBackdrop.tsx` — the brand band. Purely decorative: takes no children, reports nothing to accessibility, and is positioned over by the caller. `curve` (dome), `slant` and `motif` (`bubbles` / `cards` / `bars`) give each tab its own silhouette; `BandSurface` wraps it for bands whose height follows their content.
  - `EmptyRow` (local to `app/(tabs)/index.tsx`) — the single empty/status state, so "free time", "all clear" and "semester ended" read as one component.
- **Dashboard order** (`app/(tabs)/index.tsx`): **brand band** (app bar + semester timeline) → **stat card**, overlapping the band → getting started (conditional) → toolbelt → Today → Tasks → Upcoming → My Subjects. **When Today, Tasks and Upcoming are all empty they collapse into a single "You are all caught up" panel** rather than three headings over three empty-state cards — roughly 330pt spent saying nothing, which is what made an idle term feel like a cluttered screen.
- **Today is a timeline, not a list of boxes.** One panel, rows divided by hairlines, each with a fixed 50pt time rail (clock / AM-PM / duration), a subject colour rail, and P/A/NC. The current class is washed in the attendance tint and badged `NOW`; ended classes drop to 55% opacity. Room sits on its own line so it can never wrap under the clock icon.
- **`QuietState` is borderless and uncoloured.** It had drifted back into a tinted, bordered 72pt card with a 34pt illustration, so "Free time", "All clear" and "Clear skies" stacked up as the three most colourful things on the page — the opposite of receding. It is now a single muted row: small icon, bold title, body, on the page background. Nothing about an empty section should compete with a full one.
- **The subjects shelf is full-bleed** — the horizontal `ScrollView` breaks the page gutter and carries its own `paddingHorizontal`, so the first and last cards no longer clip at the screen edge. It snaps to card width. Each card carries the subject's colour as a **soft square holding its initial** — it was a 4dp cap ruled across the top of the card, a hard line that made a shelf of cards read like a table header — plus a **fixed-height name slot**, so cards stay the same height whether or not a subject has a name behind its code.
- **Term and timeline both live in the band.** `Tabs`' `renderTrigger(open, label)` prop lets the dashboard supply its own trigger while reusing the shared `TermSwitcherSheet` and the `SPOTLIGHT_IDS.homeTerm` anchor, so the switcher can sit on the azure as plain white type. **That trigger is deliberately unfilled**: a translucent white well behind 11.5px type lifts the local background to ~3.5:1, so the chip would look frosted and read *worse* than plain white on the band, which measures 4.7:1. The `PRO` badge is outlined for the same reason — and outlined-for-status against filled-for-action also keeps it from being a second white pill beside `GET PRO`. The timeline sits directly under the term it belongs to; with dates missing it becomes a link to Academic Manager, and a term with only one date still shows it.
- **THE HEADER IS THE IDENTITY — the brand band.** The screen used to open with a grey status board on a grey page: the semester timeline and the three headline numbers as two equal recessed tiles, nothing claiming to be the most important thing, and no brand colour anywhere. The app bar and the timeline now share **one full-bleed azure field that runs under the status bar**, with the timeline set in white on it (`Semester progress`, the percentage, the bar, then `Day n of m` and the term range). `components/dashboard/HeroBackdrop.tsx` draws it.
- **Fin in the app bar is an avatar, not a sticker.** `assets/images/fin-avatar.png` (@1x–@3x, 36dp) is the face cropped from the launcher icon `FinScholarAppLogo.png`, clipped to a circle inside a `Brand.markRing` edge. The full-body `FinLogo.png` shrunk to 27dp in a frosted well read as an emoji pasted onto the band — cap, books and outline all collapse at that size. The ring is near-opaque because the avatar's azure sits close to `heroFrom` and dissolves into the band without it. Regenerate the three densities from the launcher icon if the icon changes.
- **The hero is SVG, not `expo-linear-gradient`.** The project already ships `react-native-svg`, so the gradient, the sheen, the bubbles and the curved bottom edge are **one flattened draw** instead of a stack of overlaid views — and no new native module means no rebuild to see it. **Do not add a gradient dependency for this.**
- **The band's bottom edge is a shallow dome, not a straight cut** (`curve` — a quadratic whose control point at `(w/2, h + sag)` puts the deepest point exactly at `h`, so `sag` is the depth of the dome and not a control offset). It is the only shape on the dashboard that is not a rounded rectangle, which is what stops the header reading as one more box on the pile.
- **Hero decoration is confined to `0.2h … 0.65h`, and both edges of that window are load-bearing.** Above it: the app bar is pinned and painted a flat `heroFrom` while the band scrolls under it, so the two only read as one surface while the band's first rows are *also* flat `heroFrom` — **nothing bright may go near y=0**. Below it: the stat card overlaps the bottom ~48dp, so a sheen pooled at the curve is painted *under* the card and survives only as a sliver clipped by the card's edge, which reads as a rendering artefact rather than as depth. Both were tried.
- **The three numbers are ONE card that overlaps the band**, not three tiles in a panel. Three separate tiles made the eye count boxes before it read values, and a card sitting below the header with a gap starts a second stack; a card straddling the band belongs to the header. Columns are divided by **short hairlines inset 8pt from the card's top and bottom** — a rule that stops short of both edges groups without ruling, which is the distinction the old "no dividers at all" constraint was reaching for. Colour stays on a small squircle icon chip per column, so the accent palette is not spent three times above the fold.
- **`rowDivider` is the token that keeps the lists soft** — an **inset** rule (12pt from both edges) for list rows, positioned absolutely so it can drop into a flex row without disturbing it. Full-bleed rules between uniform rows turn Today, Tasks and Upcoming into ruled paper. (`softFill`, the recessed tile fill the old status board used, went with the board.)
- **This screen owns the status bar while it is focused.** The band is dark, so `useFocusEffect` sets `setStatusBarStyle('light')` and hands it straight back on blur — `app/_layout.tsx` sets dark icons in light mode, which would be invisible on the azure header. The loading skeleton paints the band too, so the header does not flash white-then-blue when the data lands.
- **Corner radii were raised across the dashboard** (board and first-run card to `Radius['4xl']`, list panels to `3xl`, subject cards to `2xl`, toolbelt icons to full circles, the app-bar settings button to a circle) after the screen was called "too sharp, too blocky, like an Excel sheet". Date blocks in Tasks and Upcoming are **borderless and 16pt-round**: an outlined rectangle next to an outlined card, repeated down a list, is what gave the timeline its ruled-paper feel, and the tonal fill alone carries the urgency.
- **THE ILLUSTRATED GREETING BANNER IS GONE — do not put one back.** The status board used to open with a 152pt header: SVG waves, two sparkles and `FinDashboard.png` at 170pt bleeding off the right edge, carrying a greeting and a rotating quote. It was the largest element above the fold, held no information, and sat directly under an app bar that already said "FinScholar" — two headers before any content. It was called out as gimmicky and out of place. The greeting is still **one line in the app bar** (`"{greeting}, {displayName}"`, with `displayName` the signed-in first name falling back to **Scholar** for guests), and the quote survives only in the all-clear panel, where there is genuinely nothing else to say. `getRandomQuote` still rerolls on tap there. **Colour, not illustration, is what carries the personality now** — Fin stays a 27pt mark in the app bar, and the one place he appears at full size is that all-clear panel, washed in the brand blue rather than green because it is the screen's mascot moment.
- **The app bar is the greeting, on the brand.** Fin in a frosted 38pt well, the greeting, the PRO badge, then the **term switcher as a subtitle** (carrying `SPOTLIGHT_IDS.homeTerm`) — the same app-bar anatomy as Schedule, Grades, Calendar and Tasks, in white on the azure. It stays **pinned** while the rest of the band scrolls away under it. The term name and its Switch button therefore left the status board, which is now purely the timeline: `Semester progress` + the percentage, the bar, then `Day n of m` and the end date. Long-press the greeting for the dev menu; `GET PRO` moved to the right-hand group and still opens `PremiumPaywallModal`.
- **Attendance distinguishes "not tracked" from "at risk".** Sessions left pending for over a week auto-flip to `absent`, which is right for someone using the feature and badly wrong for someone who never opened it — a five-week-old term reported **14%** in red. The tile now shows `--` / "Not tracked" until `attendanceLog` holds at least one non-pending entry.
- **The toolbelt has no container.** The grey recessed shelf it used to sit in was a fifth rectangle in the first screenful, drawing a box around three things that already read as a row. The tiles are **squircles, not circles**, so they rhyme with the card corners above instead of introducing a third shape, and Scan carries the **brand wash** rather than the generic "tools" sky — it is the flagship feature, and it pulls the header's blue down the page.
- **The toolbelt holds only what the tab bar cannot reach.** Scan, Attendance and Terms. Flashcards and Calendar were dropped from it: both are tabs sitting a thumb's width below, so half the shelf was duplicate navigation.
- **Getting started is sized to what is left.** Header with per-step pips and an `n/m` count, the single next step as a filled primary CTA, and a footer with "All steps" (expands to every step, completed ones included, so nothing became unreachable) and "Show me around".
- **The native splash must match `AnimatedSplash`'s first frame — same colour, transparent art.** `app.json`'s `expo-splash-screen` block was still pointing at `FinScholarAppLogo.png`, a **512x512 RGB PNG with no alpha channel**, on `#f8fafc` / `#0f172a`. An opaque square on a background it does not match renders as a blue sticker floating on navy — it reads as an unconfigured, default Expo startup, and then flashes to `AnimatedSplash`'s indigo. Both themes are now `#4f46e5` (the exact colour `AnimatedSplash` paints, which does not theme) with `FinLogo.png`, which **has** transparency and is square so Android 12's circular splash mask cannot crop it badly. **Any splash asset must be transparent** — check with the PNG colour-type byte, not by eye, since an opaque image looks fine on white.
- **The splash shows the APP ICON on a background matched to it**, so the launch reads as the icon growing into the app rather than a logo pasted onto a coloured card. `assets/images/SplashIcon.png` is generated with `sharp`: `FinScholarAppLogo.png` at 700px, centred on a 1024px canvas **filled with `#3991f6`** — the icon's own perimeter colour, measured by averaging a 4px band around its edge (the whole edge spans only ~6 units, which is why a flat match works). `expo-splash-screen`'s `backgroundColor` is the same `#3991f6` in **both** themes.
- **The drawable's background is baked in, not left transparent.** Android 12+ clips the splash icon to a circle; with the fill baked in, the circle's rim *is* the background colour, so the crop is invisible at any radius and no resampling halo can appear where the icon's edge meets the canvas. A transparent canvas was tried first and left the icon edge at `#449bf7` against a `#3991f6` background.
- **`AnimatedSplash` starts on the icon blue and morphs to the brand indigo** (`bgMorph` + `interpolateColor`, 520ms). It cannot simply stay blue: at 16px the subtitle reaches only ~2.3:1 on `#3991f6` versus ~4.4:1 on `#4f46e5`. Starting matched kills the hand-off flash; ending on indigo keeps the type legible. **If the splash background in `app.json` changes, change the `0` end of that interpolation with it.**
- **The launcher icon is deliberately left alone.** `icon` and `adaptiveIcon.foregroundImage` stay on the opaque `FinScholarAppLogo.png` over a `#ffffff` adaptive background. A padded transparent mascot on indigo was tried and **reverted on the user's call** — technically the more correct adaptive icon, but it looked worse. Taste wins; do not re-apply it as a 'fix'.
- **`android/` is gitignored but generated**, so an `app.json` splash change is inert until `npx expo prebuild -p android` rewrites `values/colors.xml`, `values-night/colors.xml` and the `splashscreen_logo` drawable. Editing `app.json` alone and rebuilding changes nothing.
- **Startup** (`components/AnimatedSplash.tsx`): the splash takes a `ready` prop and only animates out once the app behind it is loaded, never before a 1.5s floor. It used to fade itself out on a fixed 2.5s timer while `app/_layout.tsx` kept it mounted until fonts finished — a slow load left an invisible splash over a blank screen. It exits by lifting and fading rather than zooming, and **both it and `app/welcome.tsx` spring their mascot from 0.94 at full opacity, never 0.3 from invisible** — the native splash has already drawn Fin on the same indigo, so a mascot that fades in from nothing blinks out and pops back the instant the handover happens.
- **`useTabBarHeight()` from `components/CustomTabBar.tsx` is the one measurement of the floating bar**, and every screen with content at the bottom uses it. The bar is `position: 'absolute'`, so screens must reserve its height themselves; they used to hand-tune a number each, which drifts from the bar and silently clips the last row — Practice Test's options were unreachable behind it and Profile reserved 60pt for an 88pt bar. `academic-manager` reserved nothing at all. The hook and the bar's own `paddingBottom` share `tabBarBottomPadding()`, so they cannot drift apart. **Never hardcode a bottom inset for the tab bar.**
- **Bottom navigation** (`components/CustomTabBar.tsx`): a sliding pill painted behind the active icon; tab buttons are `minHeight: 52`. The bar is **opaque** (`theme.tabBg`) — at 95% alpha, page content ghosted through it as unreadable text behind the labels. All six destinations are unchanged.
- **Every dashboard action survived the redesign** — dev menu on logo long-press, paywall, sync indicator, settings, pull-to-refresh, term switcher, tour replay, scanner modal, term setup, P/A/NC attendance writes, task DONE writes, and all fourteen navigation targets.

## 🔍 Schedule Scanner — Two Engines (Implemented)
- **The scanner offers a choice, and the sheet opens for everyone.** `ScheduleScannerModal` presents two modes with a one-line explainer of the tradeoff:
  - **AI Scan** (PRO) — uploads to the `parse-schedule` edge function. Handles any layout, including grid timetables and angled photos. Needs internet, takes seconds.
  - **Quick Scan** (free) — `@react-native-ml-kit/text-recognition` runs entirely on-device. Instant, offline, nothing uploaded, no quota. Reads printed study loads well; weak on grid timetables.
- **The premium gate moved.** It used to block the whole sheet, which would have made the free on-device scanner unreachable. The sheet now opens for everyone; `isPremium` + `onRequestPremium` gate the AI *mode* inside it, and free accounts default to Quick Scan. **This is a monetization decision** — reverting means restoring the `SyncService.getIsPremium()` check on the Scan buttons in `schedule.tsx` / `index.tsx`.
- **Both engines emit the identical shape** (`{name, day, startTime, endTime, room, instructor}` with day/time as printed strings) and feed the same `handleScannedClasses`, so there is one import path and one conversion path — `utils/scheduleParsing.ts`.
- **A study-load row can hold SEVERAL meeting patterns.** The real format is `TH 08:00 AM - 10:00 AM, M 07:30 AM - 10:30 AM` in one Schedule cell, with `GLE202, NGE105` in one Room cell. `extractClassesFromRow` therefore returns an **array**: it finds every `DAY TIME - TIME` segment in the row and pairs each with the room at the same position. The first version assumed one day and the first two times per row, which silently dropped half the sessions of every two-pattern subject. The regression test is the user's actual table — 8 subjects → 13 segments → 14 timetable blocks.
- **The name is the course code plus its section**, not the row text. `CSIT227 G2`, `SOCSCI032 E3/D4`, `PE205 G4/G8` — the Description column is prose and was previously ending up inside block labels. The `C0` cluster column is explicitly excluded from being read as a section.
- **`ONLINE` is a valid room, not just a delivery mode**, so it is deliberately absent from the `NON_ROOM` filter; taking only the first token of each comma-separated part already drops the trailing `In-Person` / `Hybrid`.
- **`utils/ocrSchedule.ts` is the on-device reconstruction**, pure and tested (`__tests__/ocr-schedule.test.js`): flatten blocks to lines, group lines into rows by vertical band (tolerance scales with median line height), order each row left-to-right, then pull day/time/name/room out of the row text. It **requires a time** in the row and a day token that actually resolves, which is what rejects headers, totals and prose.
- **Day-token scoring matters**: multi-day codes and full words outrank a bare letter, so a section suffix like `G7` or a stray `M` cannot beat a real `Saturday` in the same row.
- **ML Kit is a native module** — adding or updating it requires a dev-client rebuild (`cd android && ./gradlew assembleDebug`, then `adb install -r -d`). The modal detects the "not linked" error and tells the user to use AI Scan meanwhile.

## 🔍 AI Schedule Scanner (Implemented)
- **It reads TWO document types**: a visual weekly timetable, and a study load / enrollment form (one row per subject, days as a code like `MWF`, time as a printed range). The prompt in `supabase/functions/parse-schedule/index.ts` names both and lets the model decide which it is looking at.
- **The model transcribes; the app converts.** The response schema asks for `day` and `startTime`/`endTime` as **verbatim strings** ("MWF", "8:00 AM"), never computed day indices or 24-hour floats. `utils/scheduleParsing.ts` does the expansion and arithmetic — deterministic, tested (`__tests__/schedule-parsing.test.js`), and free of model variance. Because nothing is left for the model to reason about, `thinkingBudget` is 0.
- **`normalizeScheduleDays` is the only day parser.** It handles numbers, arrays, full names, abbreviations (`Th`/`Thu`/`Thur`/`Thurs`/`R`/`H`), run-together codes (`MWF`, `TTh`, `MTWThF`), separated lists (`M,W,F` · `M/W/F` · `Tues & Thurs` · `mon tue wed thur`) and ranges (`M-F`, `Mon-Fri`, `Tue to Thu`). **Compressed parsing is all-or-nothing** — a token with any unrecognised letter yields `[]`, because skipping unknown characters mined day codes out of ordinary words ("TBA" → Tuesday, "nonsense" → Saturday) and invented classes from bad OCR reads.
- **It returns `[]`, not `[0]`, for unreadable input**, so a caller can tell "no days" from "defaulted to Monday" — that distinction is what stops phantom Monday classes.
- **Fixed: the two scan buttons disagreed.** `app/(tabs)/index.tsx` expanded `MWF` into three classes; `app/(tabs)/schedule.tsx` had its own single-day `normalizeDay` that silently collapsed every multi-day code to Monday. The same scan produced different timetables depending on which button started it. Both now import the shared util — **do not reintroduce a local day parser in a screen.**

- **Failures are classified, not displayed raw.** `components/schedule/ScheduleScannerModal.tsx` calls the `parse-schedule` Supabase edge function (deployed only — not in this repo), which relays the model provider's own error text. `utils/scannerErrors.ts` decides whether that text is transient (`isTransientFailure`) and what the student sees (`friendlyScanError`). A provider overload — "This model is currently experiencing high demand…" — becomes "Fin's scanner is busy right now", because "model" is not a word a student should be shown.
- **Bounded retry.** Transient failures retry up to `MAX_SCAN_ATTEMPTS` (3) with `SCAN_BACKOFF_MS` (1.2s, 3s), showing "Scanner is busy — retrying (n of 3)…" so it never looks frozen. Permanent failures (bad image, not logged in, low credit) break out immediately — never retry those, it just burns the user's time and the provider's capacity.
- **The word boundaries in that regex are load-bearing** and covered by `__tests__/scanner-errors.test.js`: without ``, a request id containing `429` would look like a rate limit and trigger pointless retries. An earlier inline version of this shipped a literal backspace byte where `` was intended, which silently disabled the whole status-code branch — hence the extraction to a tested util.
- **Images are downscaled before upload.** `expo-image-manipulator` shrinks the longest edge to 1568px at JPEG 0.7 when picking, falling back to the picker's own base64 if it fails. A 12MP photo is several MB once base64-encoded — slow to upload, and close to the edge function's request limit — and the provider downscales to ~1568px anyway. **This added a native module: a dev-client rebuild is required.**

## 🗓 Timetable Blocks & Scan Reveal (Implemented)
- **Blocks are painted SOLID**, from `ClassPalette` / `getClassColor(colorIdx, isDark)` in `constants/Theme.ts` — six indexed fills, each with an `edge` for the app's bottom lip, white text on top. They used to be a 20%-alpha wash with coloured text, which read as a smudge over the grid lines and fought the text at low zoom. The three local palettes in `TimetableGrid.tsx` (`COLORS`, `EXPORT_DARK_COLORS`, `EXPORT_LIGHT_COLORS`) are gone; the theme is the only source, so the grid, the dashboard's Today rows and the export all agree.
- **Selection** swaps the lip for a full ring (white in dark, near-black in light) and tints the check to the block's own colour.
- **Scan reveal:** `handleScannedClasses` bumps `revealToken`, which `TimetableGrid` passes to each `ClassBlock` as `appearToken`. Blocks then replay a 320ms fade + scale + rise, staggered 55ms apart in **chronological order** (`appearIndex`, computed in the same `useMemo` as the overlap layout), capped at 1.6s total. Export mode never animates — it must render instantly for `ViewShot` to capture it.
- **The post-scan hand-off is sequenced, and must stay that way.** `handleScannedClasses` closes the scanner, waits 280ms for that sheet to slide out, *then* bumps `revealToken`, and only holds the "check my work" prompt until `280 + revealMs + 400`. Firing all three on the same tick — which is how it first shipped — played the entire fill-in behind two modals, so it looked like nothing animated at all. Timers are tracked in a ref and cleared on unmount.
- **Adding an inline arrow or fresh object to a `ClassBlock` prop still kills the memo** (see Schedule Performance); `appearIndex`/`appearToken` are plain numbers for exactly that reason.
- **Scanner progress** (`ScheduleScannerModal.tsx`): the picked image is shown at 55% opacity under a sweeping indigo band with corner brackets, driven by a native-driver `Animated.loop` gated on `isProcessing`. It replaced a bare `ActivityIndicator`, which gave no sense that the student's own image was being read.

## ⚡ Schedule Performance (Do Not Regress)
- **The export tree is mounted only for the capture.** `app/(tabs)/schedule.tsx` renders the timetable at 3084px wide for image export — roughly a 2940×1920 offscreen view tree per theme, plus a 3084px `ImageBackground`. These used to be mounted whenever the tab was in grid view (the default), so every zoom, quick-edit toggle and attendance tap re-laid them out; that was the app's main source of lag. `exportRender` state now mounts them only while a capture/preview is in flight, **and only for the theme being captured**; `closeExportPreview()` drops them. Never move that back to an always-mounted condition.
- **`ClassBlock`'s `memo` depends on stable props.** The overlap layout in `TimetableGrid` is behind `useMemo([classes, displayDayIndices])`, `displayDayIndices` is memoised, `handleToggleSelect` is a `useCallback` that takes the id, and each block's room is resolved once into `displayRoom` so blocks no longer take the whole `classes` array. Before this, `processedClasses` rebuilt every class object on every render and the memo never once hit. Adding an inline arrow or a fresh array/object to a `ClassBlock` prop silently undoes all of it.
- `cls.room` stays the raw (possibly comma-separated) value because it reaches the details modal through `onPressClass`; `cls.displayRoom` is the resolved single room for the block.

## 🗓 Schedule Tab Layout (Implemented)
- **Principle:** the timetable is the content, everything else is chrome. `app/(tabs)/schedule.tsx` is a fixed-height column — app bar, view switch, actions + tools, digest, schedule — not a page that scrolls. The grid is on screen the moment the tab opens; nothing above it scrolls it out of view.
- **App bar:** identity + PRO badge, with the **term switcher as the subtitle chip** underneath. `components/ledger/Tabs.tsx` takes a `compact` prop for this; both variants open the same `TermSwitcherSheet`, so the term list has one implementation.
- **Action + tools row:** directly under the view switch — **Scan** and **Add** as compact pills on the left, then Quick Edit, the zoom stepper and Export as icon-only buttons on the right. One 34pt row carries every control the schedule has. The action pills wrap the `SPOTLIGHT_IDS.scheduleAddClass` tour anchor.
- **Digest bar:** one tappable line showing today's date, today's class count and the ongoing/next class. It replaces the old hero banner, the four-metric stat card, the "Today" strip and the "Next class" strip that used to sit above the grid.
- **Overview sheet:** everything the digest bar summarises lives here — Fin's hero card and quote, the four term metrics (today's classes, weekly blocks, attendance rate, active term), today's classes, upcoming milestones, subjects missing class times, and **Reset Schedule** as the last, destructive row. Opened from the digest bar; nothing was dropped in the move.
- **Zoom and Quick Edit appear in grid view only; Export in grid and Glass.** Attendance shows just the action pills.
- **The schedule runs to the bottom of the screen.** There is no bottom dock: whichever view is showing pads itself by the tab bar's height (the bar is absolutely positioned), which is why the screen uses `SafeAreaView edges={['top','left','right']}`. Page gutter is the `GUTTER` constant (12pt), narrower than the app's usual 16 so the timetable gets the width.
- **`TimetableGrid` takes `fillHeight`** to stretch to the space it is given instead of its default fixed 600pt box. Export mode is unaffected.
- **Empty state:** a term with no classes renders `EmptyScheduleCard` in place of an empty week, so a blank grid never reads as a bug.
- **Deep links:** `?viewMode=` accepts `grid`, `list` and `attendance`; `glass` is normalised to `list` since that is the label the switch shows for it.

## 🗓 Calendar Tab (Rebuilt)
- **Principle: a day is not just its milestones.** The tab only ever read `sem.milestones`, so tapping October 14 could not tell a student they had three classes and a lab report due — that lived on two other tabs. `utils/calendarModel.ts` now flattens the ledger into one dated stream: **events** from every term's `milestones`, **task deadlines** from `subject.requirements[].dueDate`, and **classes** derived per-date from `sem.classes` using the app's Monday-zero `day` index. Classes and tasks are read-only here and deep-link to Schedule and Tasks.
- **Two lenses, one stream.** **Month** is the grid with the selected day's timeline inline underneath it; **Agenda** is a date-sectioned list of everything upcoming (or past) with sticky headers. The day view used to be a 60%-height modal, so comparing two days took four taps.
- **The day is ONE chronological timeline, not three lists.** Untimed items sit in an all-day strip at the top; everything with a clock — classes, deadlines, exams — falls into a single time-ordered list below it. An earlier pass grouped the day into "Calendar", "Due" and "Classes" cards, which made a student interleave three sections in their head to work out what came next.
- **The month view is ONE scrolling page.** `app/(tabs)/calendar.tsx` keeps the app bar and control bar fixed and puts the month header, grid and day timeline in a single `ScrollView` padded by `useTabBarHeight()`. It was first built as a fixed column with the grid pinned above a separately-scrolling panel, and on a real phone that left the day's content stranded behind the floating tab bar with nowhere to scroll to. **Nothing inside that page may own a nested vertical scroll** — `DayPanel` is a plain `View` for exactly this reason.
- **Chrome was collapsed into two bands.** App bar (identity + term chip + sync/scan/settings), then one control bar carrying the view switch, search, filters *and* the term strip. Three stacked banded rows read as chrome, not as a calendar. The original screen was worse still: a header, a full-width term selector, an "Auto-Import Events" banner and a "Semester Duration" card — roughly 260pt before the calendar itself.
- **`utils/calendarModel.ts` is the only date authority**, covered by `__tests__/calendar.test.js`. Two live bugs went with it: event dates were compared through `new Date(str).toISOString()`, which reports the previous day for anyone west of UTC, and the grid measured cells from `Dimensions.get('window')` read once at module scope, so rotation and tablets desynced the grid from its own weekday header. `toDateKey` / `parseDateKey` / `toDateKeyLoose` are local-time only.
- **The grid is a full-bleed ruled lattice, and it is the page.** Cells are divided by `theme.cardBorder` hairlines running to both screen edges, and `MonthGrid` takes a `height` — ~72% of the measured viewport — so the month fills the first screen with the day's timeline waiting just below the fold. Three earlier passes were each wrong in a different way: every day as its own filled rounded box (blocky, muddy); no cell surface at all (a bare field of numbers, a date picker not a calendar); then a correct lattice but boxed in a small rounded card, which read as a widget bolted above a list. A month view has to *be* the screen.
- **Cell rows earn chip slots from their height:** `Math.floor((rowHeight - 26) / 15)`, capped at four. Taller rows spend the space on more of the day rather than more blank.
- **NEVER PASS A FUNCTION TO A `style` PROP — this shipped twice.** This project patches `react-native-css-interop` (NativeWind v4), whose wrapper remaps `style` and **silently drops a `({ pressed }) => ({...})` callback**. When the day cells used one they lost their `flex` and `height`: first eleven cells wrapped onto a row sitting under seven weekday headers, then the rows collapsed to the height of their digits with chips spilling past their columns. Press feedback goes on `TouchableOpacity`'s `activeOpacity`. Every other `Pressable` in this repo passes a static object; the three function styles that caused this were the only ones in the codebase. `__tests__/calendar.test.js` Suite 7c greps the components and fails on `style={(`.
- **SEVEN `flex: 1` CHILDREN PER EXPLICIT WEEK ROW — do not reintroduce `flexWrap`.** The grid was one `flexWrap` container whose cells carried a measured width with a percentage `flexBasis` fallback; the percentage never resolved in Yoga either. `chunkWeeks` cuts cells into rows and each row lays out seven flexed children, which cannot be wrong and needs no measurement at all. Suite 7b guards it across three years of months.
- **A five-pill month strip replaces the prev/next chevrons.** `MonthStrip` is always regenerated around the anchor, so the active month is permanently the middle pill and the strip never has to scroll itself into position — five pills fit a 360dp screen outright. Two arrows moved one month at a time and said nothing about where you were.
- **Monday-first, and trailing empty weeks are trimmed.** Monday because `class.day` is Monday-indexed everywhere else and a Sunday-first grid put the week's classes in the wrong column. `buildMonthMatrix` always builds six rows (the most a month can span) and `trimTrailingWeeks` drops any final row that is entirely next month — September 2026 goes from six rows to five, saving ~50pt. Fixed six rows only earned their keep while a panel below had to stay put; once the page scrolls they are waste.
- **Cells carry named chips, not dots.** Two slots per 58pt cell, each a `fill`/`ink` tinted bar with the item's own title, truncated. A dot says "something happens here" without saying what, so every question cost a tap; "Midte…" in a 48pt cell is still an answer. When a day overflows, `buildDayChips` spends the **last** slot on the count — two items show two chips, three show one chip and `+2` — so the cell can never under-report the day. Classes are deliberately left out of the grid: they repeat weekly, so a chip on every weekday is no information, and they would evict the events the grid exists to show. They are in the day timeline instead.
- **Today is a filled primary circle; the selection is a wash across the whole cell.** Two separate signals that never collide, and the circle is the one convention every calendar user already knows.
- **The term is drawn by subtraction.** Days outside `startDate`–`endDate` drop to 38% opacity; nothing inside the term is tinted. Colouring 30 cells that all mean "normal" spends a colour to say nothing. `getTermProgress` turns the two dates into "Week 7 of 19 · 66 days left" in the term strip, and the dates themselves live in `TermSheet` behind it — previously a permanent card showing two raw dates and no arithmetic. Term dates also gate class derivation, so a July Monday no longer shows a full timetable.
- **Rows are a time gutter plus a tinted block** (`CalendarRows.tsx`): a fixed 52pt clock column, then a rounded washed card with a 3pt solid left edge in the item's own colour — the same anatomy as the app's timetable blocks. They were first drawn as hairline-divided rows inside one bordered card, which made a day look like a settings screen rather than a schedule. Classes take a neutral fill with a coloured edge; a list of six solid blocks is a paint chart.
- **`components/calendar/calendarTheme.ts` is the one event palette.** `calendar.tsx` and `VisualCalendar.tsx` each declared their own `MILESTONE_TYPES` with **different hexes**, so one event was cyan as a dot and teal as a chip eight points below it. `getEventTint(type, isDark)` returns the `fill`/`line`/`ink`/`solid` shape `Tints` uses. `VisualCalendar.tsx` was deleted.
- **Events can be edited where they are.** Each item carries its own `yearId`/`semId`, so editing an event from another semester writes to that semester. The old screen could only mutate the active term and silently switched the student's whole term selection first.
- **Scope is now explicit.** The grid used to show every term's events while the list below showed only the active one, with no signal. A `This term / All terms` filter chip states the choice; search and per-type chips sit beside it. A type filter never hides task deadlines — picking "Exams" must not silently hide what a student owes.
- **One floating action button owns "add".** It sits above the tab bar and adds on the selected day (on today, in the agenda). It replaced a pill in the day header *and* a second pill in the agenda toolbar — and once the month body became a scrolling page, a header button was the thing that scrolled away exactly when a student had browsed to the day they wanted to add to. Both lists reserve `tabBarHeight + 84` so it can never cover the last row.
- **New affordances:** search, per-type filters, jump-to-Today, horizontal swipe between months (`PanResponder` — the repo has no `react-native-gesture-handler`; the 1.6 dx/dy ratio guard is what stops it stealing the page's vertical scroll), quick Today/Tomorrow/Next-week date chips in the event form, and Upcoming/Past in the agenda.
- **Every action survived** — add, edit, delete, multi-select bulk delete (now in the agenda header), AI import via `CalendarImportModal` with its `SPOTLIGHT_IDS.calendarImport` tour anchor, term start/end editing, term switching, sync indicator, settings, and the cross-term event list. The import banner was dropped; the sparkle button in the app bar and the empty state's copy are its entry points, matching how the grades tab reduced "add subject" from three permanent buttons to one.
- **Forms run on `KeyboardSheet`.** The add/edit form was a bare `Modal` at a fixed `h-[70%]` with Save pinned to the bottom and no keyboard handling, so on a short phone the keyboard covered both the note field and the button.

## 📊 Grades Tab (Rebuilt on the brand band)
- **Principle: same colour as the dashboard and study, different shape.** All three tabs open on the azure `HeroBackdrop` band under a pinned app bar, but each ends differently: dome + floating stat card (dashboard), flat + page pulled over it as a sheet (study), **slanted edge (`slant`) with a rising-bars motif (`motif="bars"`)** (grades). The user explicitly asked that tabs not read as copies of the dashboard — keep it that way.
- **`components/dashboard/BandSurface.tsx`** measures its content and draws the backdrop at that height, so a band with variable content (a term with or without dates, a subject with or without scores) never clips or leaves a hole.
- **`GwaHero` replaced `GwaSummary`.** One headline figure with a **Semester / Year / Overall** scope switch, rather than three numbers competing; the semester leads because it is what moves while scores are entered. The grading-system chip sits beside the switch (it opens Ledger settings) — the gear is still Profile.
- **`GradeScale` is the tab's signature shape**: a ruler with labelled marks (Pass 60 / 75 / 90 on the home band; the subject's own pass mark and goal on the subject band) and a pin, instead of the dashboard's progress bar or study's ring. It says where you stand against the lines that matter, not just "how full".
- **`SubjectCard` is a report-card line**: the grade in a tier-tinted tile (`getGradeTierWash`), then the name, units and **"n of m scores in"**, and a thin bar with the subject's own passing mark drawn on it. Paused subjects show a pause tile. The `···` action sheet, selection mode and props contract are unchanged.
- **Entering grades is a sheet, not inline fields.** The old subject screen was an accordion where every score row was six live targets (name, score/max, WT, +SUB, delete) and every keystroke pushed the whole ledger to Supabase. Now:
  - **Periods are tabs** (`selectedPeriodId`), each chip showing its grade and weight; only the selected period's components render.
  - Components are cards with a weight badge, their average and "n of m in"; tapping the header opens **`GroupSheet`** (name with one-tap presets, weight, delete).
  - Score rows are **read-only** and open **`ScoreSheet`**: big Score / Out of fields, /10 /20 /25 /50 /100 presets, a live percent + tier read-out, weight with its auto-split placeholder, and **Save & add another** for bulk entry. A score above the max only warns (extra credit); negative or non-numeric scores block the save.
  - **Parts (sub-items, max depth `MAX_ITEM_DEPTH` = 3)**: "Split into parts" keeps the typed score as Part 1. Parts render indented under their parent and open the same sheet.
  - Name, units, pass mark and target moved from always-live fields into **`SubjectSheet`**, reached from the name/chips on the band — a stray tap on the title no longer starts renaming the subject. Renaming still renames the subject's schedule classes.
  - **Every sheet holds a draft and commits once on Save.**
- **The outlook uses the app's two-zone card, with Fin.** It has been a full indigo wash (generic dashboard filler) and then an all-white card of grey text (flat, especially in dark mode); neither read as this app. It is now the recipe the empty states, study first-run card and FinSights share: a **tinted top zone** in the outcome's tint (emerald reached, indigo within reach, amber pass-only, red lost) holding the status title and one sentence, then a **gap bar** — the subject as 100 points split into earned (tier colour), needed, spare and already-missed, goal marked — so it shows *why* the average is what it is. The white body lists each ungraded score led by a **tinted tile holding what it needs** ("17.9 OF 20", the same tile shape `SubjectCard` uses for a grade); tapping a row opens that score's sheet (`locateItem` in `utils/gradeEntry.ts` maps outlook ids to paths). **Fin stands in the zone, never in a frame.** A cropped avatar in a white rounded box read as a sticker; he is now the transparent full-body art, 128pt, anchored to the zone's lower edge with his tail tucked behind the card body (the zone clips him and reserves `FIN_ROOM` on the right). His pose is the outcome — happy when the goal is reached, thinking (FinSights art) while it is in reach or only the goal is lost, confused when only passing or nothing is left (`FIN_POSE`). Tapping him opens FinSights. Hidden until the subject has at least one score slot.
- **The list screen uses the same recipe.** The term's subjects are **one two-zone card**, not a stack of cards: a violet zone with the title, count, units/counted summary, the Add pill (`SPOTLIGHT_IDS.gradesAddSubject`), a **standing breakdown** — one bar split by grade band with a named legend ("6 On track · 2 Outstanding · 1 No scores yet", counts from `tierBreakdown` in `utils/gradeTiers.ts`). It replaced a per-subject makeup bar whose unlabeled segments had to be matched to subjects by eye; a bar on this screen must name every segment and the All/Counted filter or the selection actions; then `SubjectCard` rows (`asRow`); then an Add subject footer strip. The unscheduled banner leads with a count tile (58pt min, padded — at 48pt its NO TIMES caption touched the edges). Empty states stand Fin in their tinted zone (`EmptyPanel`'s `fin` prop): studying for no subjects, happy for no terms, confused for no semester picked, sleeping when every subject is paused. They replace the list, so the screen never shows two Fins. When the list is empty, the empty state's button carries the add-subject tour anchor instead of the pill.
- **One vocabulary for every number on the subject screen: the tile.** A grade, a score, a component average and what a score still needs all sit in the same rounded tile (`NumberTile` in `ActiveSubjectView`), tinted by tier (`getGradeTierWash`) or by the outcome, the shape `SubjectCard` uses on the list. Plain text rows of numbers read as generic filler next to it.
  - **The selected period is a two-zone card**: a violet (grades) zone with the period's grade tile, weight and a **contribution bar** — one segment per component, as wide as its weight and filled as far as its average, so the period grade explains itself — over a footer of Edit period · Add component. The dashed end-of-list "Add component" only appears from 3 components on.
  - **Component cards open on a zone tinted by that component's tier** (red when Quizzes is weak, green when strong; neutral with no scores) holding its average tile, so a weak component reads as weak before a number is read.
  - **Score rows lead with a tile**: "18 OF 20" in the tier wash, a dashed "— OF 20" plus a + for ungraded work, and "40% PARTS" for a score split into parts. The subtitle carries the percent and tier label.
  - **Fin appears once**, in the outlook. One mascot is a character; one per card would be stickers again. For the same reason the subject header has **no FinSights button**: a cropped Fin in a near-white disc sat directly above the outlook's Fin and read as a pasted-on profile picture. The header is Back + AutoConfig; the outlook's Fin is FinSights' entry, and it shows exactly when FinSights has something to say (once there are scores).
- **`utils/gradeEntry.ts` owns the logic** — weight summaries and warnings (mirroring `Calculator`'s blank-weights-split-the-remainder rule), path-addressed tree mutations that always return a new subject, name suggestions ("Quizzes" → "Quiz 3"), score validation and `subjectOutlook`. Covered by `__tests__/grade-entry.test.js`. Put new grade-entry logic there, not in `ActiveSubjectView`.
- **AutoConfig is a `KeyboardSheet`** now (it has a text field; the centred modal had no keyboard handling). FinSights reads the outlook message.
- **`utils/gradeTiers.ts` is still the single grade-band authority** (`getGradeTier` / `getGradeTierColor` / `getGradeTierLabel`, plus `getGradeTierWash` for tile fills). Covered by `__tests__/grade-tiers.test.js`.
- **Every grades action survived** — grading-system switch (all four), Erase All Data, sync indicator, profile, paywall, term switcher, All/Counted filter, add subject (with the duplicate-name error, `SPOTLIGHT_IDS.gradesAddSubject` still on the Add pill), select-all / batch delete, per-subject pause/duplicate/delete, the unscheduled-subjects banner into Schedule, and in the subject view: name/units/pass %/target editing, full period → component → item → part CRUD with weights and warnings, per-period and per-component averages, FinSights and AutoConfig (image + text).

### Scores not entered yet (ledger setting)
- **`settings.ungradedScores`: `'zero'` (default) | `'perfect'` | `'ignore'`**, chosen in Ledger settings. Before it existed every blank score counted as 0, so the default keeps everyone's numbers where they were. Read it with `readUngradedMode(settings)` from `utils/gradeEntry.ts`; `UNGRADED_MODES` holds the labels, hints and the short phrase shown under a grade ("banked so far" / "if you ace the rest" / "graded work only").
- **Three views of one subject, because each answers a different question:** *count as 0* is what is locked in (only goes up), *count as perfect* is the best still reachable (only goes down), *leave them out* is the graded average — Canvas-style, which is what most school portals show.
- **`Calculator.calculateSubject(subject, system, exclusions, ungraded)`** picks the headline by mode but always returns `floor`, `ceiling`, `pace` (graded average) and `projected` (pace carried over what is left). `calculateSemester/Year/Cumulative(…, ungraded)` pass it through. The 4th mode `'project'` is internal: it produces **"On track for"**, which is shown in every mode because it is the one figure that is neither floor nor ceiling. The chip only appears while something is graded *and* something is still open.
- **Outside `'zero'`, subjects with no scores at all are left out of the semester/year/overall average** — otherwise an untouched subject counts as a free 100 (perfect) or pulls a graded average down on nothing. Covered by `__tests__/grade-entry.test.js` suite 5.
- **Every grade surface follows the setting**: the grades band and list, the subject screen, the dashboard GWA and the home-screen widget. The subject band's ruler and each subject row's bar **shade the range from floor to ceiling** (`GradeScale`'s `range`) with the pin/number at the chosen view, so no mode hides the other two.
- **The goal card rewords itself in perfect mode**, because the grade on screen is then the best case: "Keep an 89.7% average on the 3 scores left to stay at 85% or higher", and the needed tiles read "MIN OF 20". The arithmetic (`subjectOutlook`) is mode-independent. Banked points in its bar are coloured by pace, never by the headline.

## ✅ Tasks Tab (Rebuilt)
- **Principle: the screen answers "what do I need to do next?".** It used to answer it last. `app/(tabs)/requirements.tsx` opened on Fin's Focus Station — a Pomodoro timer filling the entire first viewport — then a ~330pt stats card, then three stacked filter rows, and only then the list. A term with **one** task took two and a half screens of scrolling to reach it.
- **Tasks are grouped by WHEN THEY ARE DUE, not by status.** Overdue → Due today → Tomorrow → This week → Later → No due date. Status (Pending / Submitted / Graded) is an administrative axis, and the old screen rendered all three headings *unconditionally*, so one submitted task produced two empty sections complete with placeholder cards. `groupTasksByDue` returns **only non-empty groups**, so an empty section cannot exist — `__tests__/tasks.test.js` TSK2.2 guards it.
- **Overdue is measured against the instant; every other boundary against the calendar day.** A task due at 11:59pm is not overdue at 2pm, but "tomorrow" has to mean tomorrow whatever the clock says. Getting this wrong is what makes an overdue count untrustworthy.
- **Finished work collapses.** Submitted and graded tasks leave the due timeline entirely and live in one **Done** section at the bottom, collapsed by default with a count. A submitted task that is past its date is never counted as overdue.
- **`utils/taskModel.ts` owns all of it** — bucketing, counts, the digest line, filtering, sorting, checklist progress — and is covered by `__tests__/tasks.test.js`. Put new task logic there, not in the screen.
- **FOCUS IS INDIGO, BREAK IS EMERALD — never amber.** `tints.tasks` is the tab's *domain* tint: correct on the app bar icon, on a "due today" warning and on the Due-today group dot. Used as a full-panel wash behind the focus timer it made the whole sheet read as a caution state and stopped looking like this app at all. Focus surfaces take `tints.schedule` (the app's own indigo), which is what the original Pomodoro card used.
- **The action is inline; the configuration is in the sheet.** The station has now been in two wrong places: as a full card it owned the entire first screen of a task list, and moved wholesale into a sheet it took starting a session from zero taps to two and removed Fin from the tab completely. `FocusBar` is a ~72pt row at the top of the list carrying Fin, the clock and Start/Pause; `FocusSheet` holds session length, break mode, custom durations and reset. **Do not move Start/Pause off the screen again.**
- **Status chips are always visible and carry their counts**, so one row is both the filter and the tally — the four numbers are on screen without opening anything. Only subject and sort sit behind the funnel, and the funnel's active badge tracks just those. Search stays behind its icon, which is conventional.
- **Edit is a visible row action again; delete is not.** The split is by frequency and consequence: editing is common and harmless, deleting is neither, so it stays in the `···` sheet rather than sitting beside a task's whole history on every row.
- **The timer's state lives on the screen, not in its sheet.** `useFocusTimer` lives in `requirements.tsx`; `FocusBar` and `FocusSheet` are only its face. Mounted inside the sheet, a 25-minute session would die the moment a student closed it to go look at their list — which is exactly when they would close it. The app bar shows the **live countdown** on the focus button while a session runs, so the timer is visible from the list without owning it. Both load-bearing behaviours are carried over unchanged: the countdown is derived from a **target timestamp** (a decrementing interval drifts and is throttled when backgrounded) and **`AppState` resynchronises on foreground**.
- **Fin's sprites are the point of the focus sheet**, and they got bigger — 120pt here versus 90pt in the old corner. `sleeping` idle, `studying` running, `happy` on completion, with the same float and celebratory bounce. `TaskOverviewSheet` and every empty state use them too (`sleeping` for no tasks, `confused` for no matches, `happy` for all-caught-up), so Fin has **more** presence after the overhaul, not less.
- **Focus can be attached to a task.** The row action sheet offers "Start a focus session", which sets `taskTitle` and opens the sheet — a session now belongs to a piece of work rather than floating free.
- **The stats card became a one-line digest plus a sheet.** Four equal-weight tiles (Total / Pending / Submitted / Graded) meant nothing stood out and the one number that changes behaviour — overdue — had to be hunted for. `getDigest` states the single most urgent fact ("2 tasks overdue" / "1 task due today" / "All caught up") in a 30pt strip with a hairline progress bar; tapping it opens `TaskOverviewSheet` with the full breakdown and Fin reacting to it.
- **One action sheet for the whole list, not two buttons per row.** Every card carried a pencil and a trash can, putting Delete one mis-tap from a task's history. `TaskActionSheet` follows the grades tab's precedent: a `···` per row opens **one** screen-level sheet holding status changes, focus, edit and delete. Swipe survives — right advances, left opens actions.
- **The checkbox is binary.** To do ↔ submitted. Grading needs a score and a component, so "Add a grade" is an explicit act that routes through `TaskFormModal`. The old checkbox cycled pending → submitted → graded → pending, which is not something a checkbox can mean.
- **The row went from ~150pt to ~62pt.** Priority is a coloured rail rather than an uppercase `MEDIUM PRIORITY` chip that outweighed the task's own title; overdue is the only thing allowed to shout. The expandable description and the interactive checklist are unchanged.
- **Chrome collapsed to two bands**: an app bar (identity + the **compact** term chip, matching every other tab — this screen was still using the full-width `Tabs` row) and one control bar carrying the digest, search and filters. Search and filters are icon toggles, as on the calendar tab.
- **Add is a FAB**, carrying the `SPOTLIGHT_IDS.tasksAdd` tour anchor, so it stays reachable at any scroll position.
- **Deleted:** `PomodoroCard.tsx`, `TaskProgressIsland.tsx`, `TaskFilterBar.tsx`, `TaskIslandCard.tsx`. `TaskFormModal.tsx`, `types.ts` and `utils.ts` are unchanged — `utils.ts` is imported by onboarding and the spotlight overlay for `triggerHaptic`, and `__tests__/tasks-redesign.test.js` covers it.
- **Every feature survived** — create, edit, delete, status changes, the grade-ledger mirror (`syncGradeItem`, including moving a task between subjects), checklist subtasks, search, subject filter, status filter, all four sorts, pull-to-refresh, the `?subjectId=` deep link, sync indicator, term switching, the guided-tour anchor, and the full Pomodoro (focus/break, presets, custom length, +5m, reset, session count, drift immunity).

## 🎴 Study Tab — Anki, Simplified (Rebuilt)
- **The model is Anki's, with the setup taken out.** Decks hold notes; a note makes one or more cards; each card is scheduled on its own; the day's work is *new cards up to a limit + learning cards + reviews due*. What is gone is everything a student has to configure before it works: one options screen for all decks instead of presets, one scheduling knob instead of eight, four note types instead of a template editor.
- **Note types** (`components/study/notes.ts`): **Basic** (1 card), **Both ways** (2 cards, `ord` 0/1), **Cloze** (one card per `{{cN::…}}` number, with `::hint` and nesting), **Type answer** (Basic, but the answer is typed and diffed character-by-character like Anki's `{{type:}}`). `parseCloze` keeps malformed syntax as literal text rather than swallowing the note.
- **There is no notes array.** A note's fields live on every one of its cards (`front`/`back`; for cloze they are Text and Extra) and siblings are found by `noteId`. The ledger stays an array of ordinary cards, so an older build still reads it. Cards without `noteId` are legacy one-card Basic notes and need no migration. `buildNoteCards` reuses the scheduling of any sibling whose `ord` survives an edit — rewording keeps progress, adding `{{c3::}}` adds exactly one card, deleting `{{c2::}}` removes exactly one.
- **Scheduling is FSRS-6** (`components/study/scheduler.ts`) with Anki's default parameters, learning steps (`1 10` min), relearning steps (`10` min), fuzz, and Anki's Hard-on-first-step rule (halfway to the next step). **The only real knob is target retention** (80/85/90/95%). Old SM-2 cards convert on their next answer with `memoryFromSm2` (Anki's own conversion: stability = interval, difficulty from ease), so nobody's progress resets. `applyRating` is kept as the entry-point name; `answerCard` returns the extra flags (graduated, lapsed, leech).
- **Fuzz is seeded from the card, not `Math.random`.** The interval printed on a button is exactly the interval that button applies — a test asserts it for 25 cards × 3 ratings.
- **Study days roll over at 4 AM** (`ROLLOVER_HOUR`), not midnight. Review cards come due at the *start* of a study day, so a card answered at 11:30 PM with a 1-day interval does not reappear half an hour later. Learning cards stay minute-precise. Streaks and the daily goal still use calendar days.
- **Daily limits are per deck** (`deck.daily` = `{date, newDone, reviewDone, newBonus}`): 20 new / 200 reviews by default. Counts shown anywhere cap to what is left today, and a note shows **one card per day** — answering a card buries its new/review siblings until tomorrow (`burySiblings`), as Anki does.
- **A session is not a shuffled array any more.** `buildQueue` fixes new + review cards when it starts (reviews by due date, new cards mixed in evenly); `pickNext` picks learning cards *live*, because their due times move with every answer, and shows one due within `LEARN_AHEAD_MS` (20 min) rather than ending the session. The old "card 7 of 20" counter was wrong the moment a card was failed twice; the header now shows Anki's three counts (new · learning · review, current one underlined).
- **Colour code, used everywhere** (`studyTheme.ts`): new = blue (`tools`), learning = orange (`tasks`), review = green (`attendance`); Again/Hard/Good/Easy = red/amber/green/blue. The band gets lightened variants because the tint inks drop below 3:1 on azure.
- **Home shares the dashboard's colour, not its layout.** The same azure `HeroBackdrop`, but ended flat (`curve={0}`) with a fanned-cards motif (`motif="cards"`) instead of Fin's bubbles, and the page slid over it as a rounded sheet (`SHEET_LIFT`) rather than a stat card straddling a dome. The band carries the one number that matters — due today, with a time estimate from the student's own pace — beside a daily-goal ring (`components/study/GoalRing.tsx`), then a full-width white Study now button. Below: streak and 30-day true retention as two tinted pills, the practice toolbelt (practice test · speed match · exam prep · custom study) as a 2×2 grid of icon-beside-label tiles, then the deck list as **one panel with Anki's three number columns**, not a stack of cards. Level/XP moved into the stats sheet. Keep the two tabs visibly different: the user asked for this after the first version read as a copy of the dashboard.
- **Review = Anki's layout, not a flip.** The question stays on screen and the answer appears beneath a rule, so it is read against the question; a flip would hide the very sentence a cloze tests. Cloze replaces the face with the sentence, deletion highlighted in place. One wide "Show answer", then four buttons with the interval printed on each. The `···` sheet has edit, flag, bury, suspend, flip-sides, and card info (stability, difficulty, recall-now).
- **Every write is serialised and ignores its own echo.** `pushLocalChanges` fires the data-change listener, which reloads from storage; mid-session that reload could land after a newer answer and silently undo it. `persist` updates state first, chains writes, and `loadDecks` skips while `pendingWrites > 0`. **Do not reintroduce awaiting `SyncService` between cards** — every save also regenerates the widgets.
- **Undo is a snapshot of the whole `decks` array** (plus stats and session), because an answer now touches the card, its siblings (burying) and the deck's daily counts. One step, as before.
- **Exam mode replaced the old exam plan.** The Cepeda session list only set every card due once and then handed back to normal spacing. Now `deck.examDate` caps every interval to the day before the exam (`intervalCap`), raises the new-card pace so all unseen cards are introduced in time, and `planExamReschedule` spreads reviews that would land after the exam across the days left. It switches itself off once the date passes.
- **Custom study** (Anki's): review ahead 7 days · +10 new cards today (`daily.newBonus`) · cram (all cards, shuffled, **no rescheduling**; Again re-queues four cards later).
- **Stats sheet**: 14-week heatmap, true retention (review-card answers only — learning cards are meant to be failed), 7-day forecast, new/learning/young/mature/suspended breakdown (mature = interval ≥ 21 d). The log is `stats.log`, keyed by study day and bounded to 120 days. **`updateStudyStats` must spread its input** — it used to rebuild the object and silently wiped the log on every answer.
- **Card browser** groups by note, filters (new · learning · due · flagged · suspended · leeches), bulk suspend/unsuspend, swap sides, delete. Delete moved off each row into the note editor (and bulk), so it is never one mis-tap from a card's history. Leeches (8 lapses, then every 4) prompt to edit or suspend.
- **First run** has a "Try a sample deck" path (`SAMPLE_NOTES`) that teaches the four note types by being made of them.
- **Still true from the previous pass:** `KeyboardSheet` for every sheet with a text field (padding behaviour on both platforms, scrolling body, `keyboardShouldPersistTaps="handled"`, primary action in `footer`); the eleven-button deck alert is a grouped sheet; `manage` stays folded into `detail` — do not reintroduce a second card list; every full-screen view scrolls and reserves `useTabBarHeight()`; "Add & start another" keeps the editor open.
- **Tests:** `__tests__/study-anki.test.js` (scheduler, cloze, notes, queues, exam mode, stats, import). The SM-2 assertions in `study.test.js` / `challenger-study-adversarial.test.js` were rewritten for FSRS; everything else in them is unchanged.

### Import (reviewed and rebuilt)
- **`analyzeImport(text, separator, kind, deckCards)` in `components/study/utils.ts` is the single source** for the sheet's live preview and for the import itself. It returns the notes, the card count (Both ways = 2, cloze = one per *distinct* number), every skipped line with its line number and reason, the duplicate count and whether a header row was dropped. Covered by `__tests__/study-import.test.js`.
- **Nothing is dropped silently any more.** Before: a "Front, Back" header row became a card (and the placeholder itself showed that header), lines with no separator or an empty side vanished, the wrong separator imported nothing with no explanation, and re-importing a list duplicated every card. Now the header is recognised and left out, skipped lines are listed with why, and duplicates (against the deck and within the list, ignoring case and spacing) are counted and left out.
- **Separator defaults to Auto** (each line read with whichever of tab, `|`, `;` or `,` it uses). Tab lines take column 2 as the back and drop later columns (Anki's tags). A byte-order mark is stripped. **A single quote only opens a CSV field when it has a closing partner before a comma or the end** — "'tis" and "'90s music" used to open an unclosed quote and disappear; `'a, b', c` still parses.
- **Cloze lines can carry an Extra** (a cloze note's `back`, shown with the answer). `splitClozeLine` starts it at the first tab, `|` or `;` **after the last `}}`**, so separators inside the sentence are left alone; a comma only splits a **double-quoted** sentence (`"The {{c1::heart}}, not the lungs, pumps blood", Extra`), because cloze sentences are full of commas and an unquoted comma line must stay whole, as it always did. Tab covers Anki's plain-text export and a two-column spreadsheet paste.
- **A picked file loads into the text box.** It used to be parsed once on the side, so changing the type or separator afterwards cleared it and the file's contents were lost.
- **The sheet explains by example**: one line of rule, a two-line sample with the separator highlighted, what it makes, and a collapsible "Spreadsheets, fill-in-the-blank and other formats" section. There is no Preview step — the preview is live and the footer button says how many cards will be added.

## ✋ Quick Edit: Direct Manipulation (Implemented)
- **Blocks are dragged, not nudged.** Quick Edit used to be tap-to-select plus a bulk toolbar of ±5-minute and ±1-day buttons, so moving a class two and a half hours was **thirty taps**. Press and hold a block (180ms) to pick it up, then drag it to any day and time; drag the grab bar on its bottom edge to change how long it runs. Both snap to the same five-minute grid the nudge buttons use, so the two ways of editing agree.
- **The gesture is `PanResponder`, deliberately.** The project has no `react-native-gesture-handler`, and adding one would force a dev-client rebuild. `ClassBlock` owns two responders: the move one refuses the gesture until the block has been lifted (`onMoveShouldSetPanResponderCapture`), and the resize one only claims on a clear vertical drag from the handle — otherwise the grid's two nested ScrollViews would never scroll again.
- **`draggingId` freezes both ScrollViews** for the duration of a drag. Without it the grid scrolls under the block being moved and the gesture fights itself.
- **`utils/quickEdit.ts` holds the arithmetic** — `snapToGrid`, `resolveBlockMove`, `resolveBlockResize` — because a gesture cannot be exercised by the test runner but the clamping is what stops a block landing on hour 25 or going to a negative duration. Covered by `__tests__/quick-edit.test.js` (18 assertions, including zero-width grids and blocks longer than the day).
- **Feedback while dragging:** the block lifts (scale 1.04) and swaps its lip for a white ring, a dashed ghost marks where it came from, and a pill at the top of the grid reads out the target slot (`Wednesday · 10:00 AM – 1:00 PM`). Snapping is applied *during* the drag, not on release, so the block visibly clicks into each slot.
- **The drag moves the block by re-rendering it in the target slot, never by a transform offset.** The first version translated the block and zeroed the offset on release, which put the block back at its old base for the frame or two before the parent's state update returned — every drop visibly rubber-banded home and then jumped to its destination. `ClassBlock` now holds a `ghost` slot that overrides the props while the drag is in flight *and* while the commit settles, clearing itself only once `cls.day` / `startHour` / `duration` agree (with an 800ms bail-out if the edit never lands). Because the drag snaps, this re-renders on slot changes, not on every touch event. **Do not reintroduce a translate-and-reset drag.**
- **Edge auto-scroll.** Manual scrolling is off while a block is in the air — otherwise the grid slides out from under the finger — so dragging into the top or bottom 76pt of the viewport scrolls the grid instead, 14px per 16ms tick. Each tick re-runs the block's own resolver, and `dragScroll.delta()` feeds the scrolled distance back in as extra drag, so the block stays under the finger rather than riding the content away. The viewport origin is re-measured on every pick-up, since the chrome above the grid changes height.
- **The Quick Edit panel no longer covers the schedule.** It was a two-line card pinned over the last hours of the day. It is now a single 38pt line that can be dismissed, it is hidden entirely while a block is being dragged (it sits exactly where a downward drag ends up), and the grid's scrollable content grows by `QUICK_EDIT_PANEL_SPACE` while the mode is on so anything underneath can be scrolled clear.
- **One-step undo** covers the last drag, resize *or* bulk nudge — direct manipulation makes an accidental change much easier, so the change has to be reversible. It clears when Quick Edit closes.
- **`ClassBlock`'s memo still holds.** The three new props (`onDragStateChange`, `onDragPreview`, `onCommitDrag`) are `useCallback`s with empty deps that read `classes` and `onUpdateClass` through refs — an inline arrow there would undo the whole optimisation (see Schedule Performance).
- **Everything the toolbar did survives**: select, select all, ±day, ±time, ±length and bulk delete. It is restyled to theme tokens, and while nothing is selected it explains the two gestures instead of sitting empty.

## 🧩 Home Screen Widgets (Rebuilt)

- **Five widgets, one design system.** `FinScholarWidget` ("Up Next"), `FinScholarAgendaWidget` ("Today"), `FinScholarDeadlinesWidget`, `FinScholarFocusWidget` ("Next Class", a 4x1 strip) and `FinScholarStatsWidget` ("Semester Pulse"). Each is declared in the `react-native-android-widget` block of `app.json` **and** in `WIDGET_REGISTRY` (`widget/WidgetTaskHandler.tsx`); a test fails if the two drift. Adding a widget needs a native rebuild.
- **What replaced the old one.** The previous widget was a single indigo gradient card with a wavy SVG divider, avatar initials and a 28dp circular badge that clipped any countdown longer than "2h". Its padding was mixed per edge (12/10/2/8), its corner radius was a fixed 32 at every size, and its row count came from three hard-coded height thresholds. All of that is gone.
- **The new look.** One padded card per widget (`widgetUi.surfaceStyle`) with a barely-there vertical gradient, and **exactly one filled block** — the hero panel on Up Next, painted in that class's own colour. Everything else is quiet: a 3–4dp colour rail, a fixed time column, the name, and a right-hand column. At widget scale a stack of filled pills turns into a smudge; one solid panel against plain rows gives a figure/ground the eye resolves at a glance.
- **Class colour is shared with the app.** `widget/widgetTheme.ts` mirrors `ClassPalette` from `constants/Theme.ts` and resolves by `colorIdx` (falling back to a name hash for classes that predate it), so a class painted indigo on the timetable is indigo on the home screen. A test asserts the two tables stay identical.
- **Geometry is computed, never guessed.** `widget/widgetLayout.ts` turns the launcher's `widgetInfo` into a scale (`xs`/`sm`/`md`/`lg`) with a gutter, radius, gap and type scale; `fitCount` decides how many rows actually fit in the space left over, and `distributeRowHeight` shares out the surplus so a tall card does not end in a dead band. The suites sweep a size matrix and assert the composed height never exceeds the widget — a clipped row fails the tests, not the phone.
- **Degradation is deliberate.** Narrow cards drop the room column before they clip a class name; short cards drop the header, then the hero (Up Next falls back to the compact strip); Semester Pulse drops to one row of tiles rather than showing four unlabelled numbers; Today windows onto the first class that has not happened yet.
- **`allowFontScaling={false}` on every `TextWidget`.** The library maps a scaling font to SP, so a phone set to "largest text" pushed rows straight out of a fixed-height card. DP keeps the drawn layout the size the metrics promised.
- **Nunito is bundled for widgets** (`assets/fonts/Nunito_{400Regular,700Bold,800ExtraBold}.ttf`, listed in the plugin's `fonts` array). It is loaded as a custom typeface, and Android does **not** fall back glyph by glyph — so widget copy stays inside Latin-1 plus `· – — …`. A symbol like ✓ or ☀ would draw as a tofu box; empty states use a small rule instead of a glyph.
- **Light/dark is drawn, not detected.** Every widget renders twice as `{ light, dark }`; the library saves both bitmaps and Android picks by system theme. The old code read `Appearance.getColorScheme()` inside the headless task, which is unreliable there and never fires again once the app is shut.
- **Deep links use `OPEN_URI`, never `OPEN_APP`.** The native provider ignores `clickActionData` for `OPEN_APP` and just relaunches the app on its last screen — the old widget's attendance deep link never actually worked. Up Next → the timetable, Today → the attendance day view, Deadlines → Tasks, Semester Pulse → the dashboard.
- **One read of the ledger feeds all five.** `widget/widgetData.ts` is pure (`buildWidgetSnapshot(ledger, { yearId, semId, now })` → `WidgetSnapshot`) and holds the schedule maths, due-date urgency, attendance, term progress and grade formatting. That is what makes the widget layer testable without rendering, and it means five widgets cost one parse rather than five.
- **The in-app gallery** (Settings → Home Screen Widgets) is a paged `WidgetPreview` of all five, driven by `WIDGET_GALLERY` and `buildDemoSnapshot()`, so a student sees the real layouts before adding one.
- **Refresh cadence:** `updatePeriodMillis` is 1,800,000 (Android's floor); the app also refreshes on launch, on theme change, and after every `SyncService` data change. Countdowns can therefore go stale between ticks, which is why the hero leads with the **absolute** time and treats the countdown as a chip.

## 🚀 Monetization & Premium (Implemented)
- **Current State:** The app integrates Google Play Billing via RevenueCat (`react-native-purchases`).
- **Subscription Details:**
  - **Type:** Auto-renewing Monthly Subscription
  - **Product ID:** `finscholar_premium` (Configured identically in Google Play Console and RevenueCat)
  - **Base Plan ID:** `premium-monthly`
  - **Price:** PHP 60.00/mo
  - **RevenueCat Entitlement:** `Premium`
  - **Service Account Email:** `revenuecat@gen-lang-client-0689315991.iam.gserviceaccount.com`
- **Premium Features:** 
  - Premium unlocks advanced **AI features** (like the Schedule Scanner).
- **Early Adopter Perk (First 50 Users):** 
  - A Supabase PostgreSQL trigger automatically grants `is_premium = true` to the first 50 users who sign up in the `profiles` table.
  - The frontend `SyncService` fetches this flag. If the user is premium, they bypass the RevenueCat paywall entirely.
  - *Note: Privacy policies and Google Play declarations must reflect that you process payments and offer digital goods.*

## 📣 Release Notes & Update Prompt (Rebuilt)

- **The changelog is data, not JSX.** `constants/changelog.ts` holds every release newest-first (`version`, `date`, `headline`, `items[]`), each item colour-coded by a `Tints` key so an entry is tinted by the area of the app it touches. It used to be a hardcoded list inside the modal, which is how 1.0.7 shipped still advertising flashcard features from 1.0.5.
- **A test stops it going stale.** `__tests__/release.test.js` asserts `LATEST_RELEASE.version === app.json version`, that `package.json` agrees with `app.json` (they had drifted 1.0.5 vs 1.0.7), that entries are unique and ordered newest-first, and that every item has a real description and a valid tint. **Bumping the version without writing release notes now fails `npm test`.**
- **"What's New" is theme-aware and reopenable.** The sheet was dark-only — `BlurView tint="dark"` over a glass background with white text — so in light mode it dropped a black slab over the app, and its stacked drop shadows were the floating-template look the rest of the app moved away from. It is now built on `getTheme`/`Tints`/`Radius`/`Typography` like every other surface, with a grab handle, a version + date line, and an **"Earlier updates"** section. `ChangelogService.open()` (same imperative shape as `AlertService`) drives it from **Settings → Help → What's New in vX.Y.Z**, so release notes are no longer a one-shot popup you can never get back to.
- **The action button is pinned, not scrolled.** The old sheet kept its only button at the bottom of the scroll view, so on a short screen you had to scroll a list you had already read just to dismiss it.
- **`UpdateWarningModal` was never mounted.** It was imported in `app/_layout.tsx` and never rendered, so nobody had ever seen it — which is why its `LAST_SEEN_VERSION_KEY` snooze was declared and never wired, and why its `CURRENT_CHANGELOG` array was dead. It is now mounted, themed to match, and rewritten:
  - **"Later" actually remembers.** The dismissed remote version is stored under `@update_snoozed_version`, so the prompt asks once per release instead of on every cold start.
  - **It names both versions** — "You have 1.1.0 → Latest 1.2.0". "A new version is available" on its own gives the user nothing to decide with.
  - **Mandatory updates are supported.** An optional `force_update` column on `app_versions` hides "Later", the backdrop and the grab handle, and re-titles the sheet "Update Required".
- **Version comparison lives in `utils/appVersion.ts`**, not in the component, so it is testable. It tolerates a leading `v`, a build suffix and a missing patch part, and treats anything whose first segment is not numeric as unparseable — otherwise a bad `app_versions` row reading `"latest"` would parse as `0.0.0` and prompt on every launch.

## 🛠 Development Environment
- **Emulator:** The user uses **LDPlayer** — it reports itself as `emulator-5554`, spoofs a *ROG Phone 7 Ultimate*, and runs **Android 14 (API 34) on x86_64** (with arm translation in `abilist`). Because it is API 34, the Android 12+ splash rules apply: the splash icon is clipped to a circle, so splash art must be square with its content inside the centre ~66%.
- **ADB:** LDPlayer answers on `127.0.0.1:5555` *and* `emulator-5554`, which are the same device — connecting to both leaves two entries and makes every `adb` call ambiguous. Disconnect one and pass `-s emulator-5554`.
- **`gradlew installDebug` FAILS on LDPlayer** with `INSTALL_FAILED_MEDIA_UNAVAILABLE: Failed to restorecon`. Gradle's install path does not survive LDPlayer's storage layer. Build and install as two steps instead:
  ```
  cd android && ./gradlew assembleDebug
  adb -s emulator-5554 install -r app/build/outputs/apk/debug/app-debug.apk
  ```
  **Never pipe a Gradle build through `tail`** — the pipe's exit status is `tail`'s, so a failed build reports success.
- **JDK:** `JAVA_HOME` points at Microsoft OpenJDK 17, but a Java 8 JRE is earlier on `PATH`. Gradle reads `JAVA_HOME` so it builds fine; anything invoking `java` directly needs the path set first.
