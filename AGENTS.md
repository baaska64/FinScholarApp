# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Agent Behavior Rule
Whenever a major architectural decision, business logic change (like monetization), or third-party integration is made, you MUST proactively update `APP_CONTEXT.md` to reflect the new state of the app WITHOUT the user having to ask you to do it.

@AGENTS.md
